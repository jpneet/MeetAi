// Type declaration for the Stream Video React SDK CSS side-effect import.
// The SDK ships the stylesheet at dist/css/styles.css (mapped via the
// "./dist/css/*" exports entry) but provides no accompanying .d.ts file,
// causing ts(2882). This declaration resolves that error.
declare module "@stream-io/video-react-sdk/dist/css/styles.css";
